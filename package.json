{
  "name": "bg-iptv",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-playlist.mjs",
    "generate:template": "node scripts/generate-playlist.mjs --template --output playlist.m3u && node scripts/generate-playlist.mjs --template --output playlist.m3u8",
    "update": "node scripts/login-and-generate.mjs"
  }
}
